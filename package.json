{
  "name": "pi-searxng",
  "version": "1.0.3",
  "description": "A minimalist SearXNG web search extension for Pi",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "prepublish": "npm run clean && npm run build"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-tui": "^0.78.0",
    "@sinclair/typebox": "*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shamangeorge/pi-searxng.git"
  },
  "keywords": [
    "pi-extension",
    "pi-package",
    "pi",
    "searxng",
    "web-search"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "shamangeorge <shamangeorge@fruitopology.net>",
  "publishConfig": {
    "access": "public"
  }
}
